# Module 3 · Feedback

Two minutes to make Module 4 better.

## Reflect first (for you)

- Can you state the belief — *tracing makes bugs legible, not rarer* — and explain the
  witus-triage bug to someone else without looking? If the fail-soft-hides-bugs idea is
  fuzzy, re-read Lesson 25.
- In the lab, did you write the assertion that *only* the trace could satisfy (the strong
  draft scoring 0)? That moment is the whole module.

## Tell us (for the course)

Rate 1–5 and add a sentence where it helps:

1. **Clarity** — did "the output can't debug a loop, the trace can" land?
2. **Pace** — too slow / right / too fast across the seven lessons?
3. **The real bug** — did the witus-triage story make fail-soft-hides-failure concrete?
4. **Offline diagnostics** — did learning to read a *local* trace (no account) work for you?
5. **The four shapes** — can you tell wasted / drift / non-convergence / masking apart?

**One thing to cut. One thing to add. One thing that confused you.**

> Submission channel is set at launch (see `plans/user-tasks/` video-host task).
> Until then, open a repo issue labeled `course-feedback`.
