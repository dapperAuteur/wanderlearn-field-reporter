# Module 2 · Feedback

Two minutes to make Module 3 better.

## Reflect first (for you)

- Can you state the belief — *writing checks an LLM can actually score is a craft* —
  and name the three failure modes (vague / overlapping / compound) without looking?
- In the lab, did you catch the *overlap* by hand? That is the one no linter finds;
  if it slipped past you, re-read Lesson 15.

## Tell us (for the course)

Rate 1–5 and add a sentence where it helps:

1. **Clarity** — did the three failure modes feel distinct and memorable?
2. **Pace** — too slow / right / too fast across the six lessons?
3. **The lab** — did diagnosing the broken rubric build real skill?
4. **The linter** — did shipping `findCompoundCriteria` (and seeing its limits) land?
5. **Data-as-policy** — is it clear why weights + pass rule belong in data, not the node?

**One thing to cut. One thing to add. One thing that confused you.**

> Submission channel is set at launch (see `plans/user-tasks/` video-host task).
> Until then, open a repo issue labeled `course-feedback`.
