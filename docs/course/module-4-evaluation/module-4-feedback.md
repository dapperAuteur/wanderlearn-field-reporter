# Module 4 · Feedback

Two minutes to make Module 5 better.

## Reflect first (for you)

- Can you state the belief — *the runtime rubric is also the offline test* — and explain why
  reuse (not a second scorer) is what makes the number trustworthy?
- In the lab, did your eval actually *fail loudly* on the poisoned run, and did you see the fake
  0% when you removed the error marker? That contrast is the module.

## Tell us (for the course)

Rate 1–5 and add a sentence where it helps:

1. **Clarity** — did "reuse the critic as the eval" land?
2. **Pace** — too slow / right / too fast across the six lessons?
3. **The lab** — did building a loud-failing regression eval feel real?
4. **Pairwise** — did proving the loop beats single-shot (and when it doesn't) click?
5. **Thresholds + small datasets** — clear why margin and curation-over-volume matter?

**One thing to cut. One thing to add. One thing that confused you.**

> Submission channel is set at launch (see `plans/user-tasks/` video-host task).
> Until then, open a repo issue labeled `course-feedback`.
